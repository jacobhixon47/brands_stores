<% if @stores.any? %>
<h3>All Stores:</h3>
<ul>
  <% @stores.each() do |store| %>
    <li><a href="/stores/<%= store.id %>"><%= store.name() %></a></li>
  <% end %>
</ul>

<% else %>
  <h3>There are no stores yet!</h3>
<% end %>
